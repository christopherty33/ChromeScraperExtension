chrome.runtime.onInstalled.addListener(()=>{console.log("Web Scraper Extension Installed")});chrome.runtime.onMessage.addListener((message,sender,sendResponse)=>{if(message.action==="scrape"){let data=document.body.innerText;sendResponse({scrapedData:data})}});document.getElementById("start-scraping").addEventListener("click",()=>{chrome.tabs.query({active:true,currentWindow:true},tabs=>{chrome.scripting.executeScript({target:{tabId:tabs[0].id},function:scrapePage},result=>{console.log("Scraped Data:",result)})})});function scrapePage(){let scrapedData=document.body.innerText;console.log("Scraped Data:",scrapedData)}